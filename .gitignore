node_modules/
dist/
build/
.cache/
.env*
# …sauf l'origine canonique : publique, et un build correct ne doit pas dependre
# d'une variable que quelqu'un doit penser a definir.
!.env.production
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/.figma/design-context/

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Local design-hook cache, not project source
.impeccable/
